#import "@preview/tsinswreng-auto-heading:0.1.0": auto-heading
#let H = auto-heading;
#import "_Common.typ": *
#show: _Show
\-\-\-

name: tsinswreng-elucidate

description: 如何把東西寫得易懂

\-\-\-
#let Qb(Cont) = quote(block: true)[#Cont]
#H[使用規範的現代漢語書面語][
	#H[句子結構要完整][
		不能缺少該有的成分。

		#H[不要缺主語][
			錯誤示例:
			#Qb[
				❌點擊保存按鈕後,
				會把修改保存到數據庫。
			]

			正確示例:
			#Qb[
				✅*用戶*點擊保存按鈕後,
				*系統*會把修改保存到數據庫。
			]
		]

		#H[不要缺謂語][
			錯誤示例:
			#Qb[
				此項目名叫MyProj,
				個人項目。
			]
			正確示例:
			#Qb[
				此項目名叫MyProj,
				*是*個人項目。
			]

		]

		#H[禁止使用破折號][
			//後面綜合示例有涵蓋 此處還未想好合適的示例就先不寫了
		]

		#H[禁止濫用引號或括號作解釋說明][
			//後面綜合示例有涵蓋 此處還未想好合適的示例就先不寫了
		]

	]

	#H[規範用詞][
		#H[不要強行把雙字詞砍成單字][
			錯誤示例:
			#Qb[包間互引用 `workspace:*` 語法——引同倉成員、版本跟着走、不從 npm 拉。]
			正確示例:
			#Qb[
				包之間互相引用用的是 `workspace:*` 語法,
				引用的就是同一倉庫裏的其他成員包，
				依賴版本由倉庫內該包的當前版本決定，
				不需要從 npm 拉取。
			]

		]

		#H[避免自造抽象術語][
			錯誤示例:
			#Qb[此模塊管容器的生滅]
			正確示例:
			#Qb[此模塊管理容器的創建與銷毀]
		]
	]

	#H[指代要明確][
		#[避免濫用「他」「其」「這」「前/後者」等代詞,
			尤其是在上下文中概念較多時。
			即使你做到了指代無歧義,
			濫用代詞依然會增大讀者的閱讀負擔。
			應指明對象。]

		錯誤示例:
		#Qb[❌服務器收到請求後把它交給後端,
			後端處理它,
			再把它返回給前端。]
		正確示例:
		#Qb[✅服務器收到請求後把請求轉交給後端,
			後端處理該請求,
			再把處理結果返回給前端。]
		#[指代對象要指名詞,
			讀者不用回頭猜「它」是誰。]
	]

	#H[說清動作對象][
		如 回覆、傳達 等詞,
		說清是誰給誰。
	]

	#H[不使用不明確的縮寫/代號等][
		錯誤示例:
		#Qb[
			MyProj是某某比賽一等獎項目（1/114514）。
		]
		正確示例:
		#Qb[
			MyProj是某某比賽一等獎項目,
			在 114514 個參賽項目中排名第一。
		]
	]


	#H[避免過度口語化][
		#[文檔正文要使用書面語,
			不要把日常交談的口吻寫進正文。]
		錯誤示例:
		#Qb[❌這玩意負責把數據搞到數據庫裏去。]
		正確示例:
		#Qb[✅此組件負責把數據寫入數據庫。]
	]

	#H[除非術語或專門稱呼,
		避免夾雜英文][
		錯誤示例:
		#Qb[❌調用方只依賴抽象, 不care內部實現細節]
		正確示例:
		#Qb[✅調用方只依賴抽象, 不關心內部實現細節]
	]
	#H[避免憑空否定][
		先直接看例子:

		#[
			問: 你是哪國人?

			錯誤示例:
			#Qb[❌答: 我不是日本人,
				不是韓國人,
				而是中國人。]

			正確示例:
			#Qb[✅答: 我是中國人。]
		]

		讓你介紹一樣東西時
		你就直接說「他是甚麼」/「他怎麼樣」,
		若沒人提到xxx 你就不要憑空否定,
		不要說「他不是xxx而是yyy」。


		僅在需要澄清真實的誤解或混淆項時纔使用,
		禁止做無意義的[憑空捏造出一樣東西再去否定]。

		判別標準:
		被否定的那個理解必須是已經出現過的,
		或確確實實容易引起混淆的。

		例:
		+ #Qb[
				- 問: 我看你長得像東亞人, 你是日本人嗎?
				- ✅答: 我不是日本人。我是中國人
			]
		+ #Qb[
				- 問: 軟刪除是甚麼
				- #[答: ✅軟刪除是把數據中的記錄標記爲已刪除, 而不會真的物理刪數據的記錄
						✅ 先說是甚麼, 再否認易誤解項]
				- #[答: ❌軟刪除不會真的物理刪數據的記錄, 而是把數據中的記錄標記爲已刪除
						❌ 問甚麼就應該先答甚麼
					]
			]
	]
]

#H[行文][
	#H[注重邏輯連貫, 注重過渡][
		- #[句子之間、段落之間的因果、順序、轉折關係要寫明,
				不要讓讀者腦補。]
		- #[用「先...再...」「若...則...」「因此」「另一方面」等連接詞點明關係。]
		錯誤示例:
		#Qb[服務器收到請求。
			緩存命中就返回。
			沒命中就查數據庫。
			數據庫也沒有就返回404。]
		正確示例:
		#Qb[服務器收到請求後先查緩存。
			若緩存命中,
			直接返回緩存結果;
			若未命中,
			再查數據庫,
			數據庫中也沒有則返回404。]
	]

	#H[明確中心主旨/主題/寫作目的][
		文章邏輯要連貫,
		銜接自然,
		文章的每一部分都應爲中心主旨服務,
		和寫作目的/寫作主題有關聯。
		不能想到甚麼就說甚麼。
		不要讓用戶疑惑「我明明在看關于xxx的文章,
		現在卻看的是yyy的內容,
		你寫的這些yyy和xxx有甚麼關係」

		常見的病是相關內容放錯位置:
		開頭向讀者許諾一條主線,
		中途整段鋪開通用背景,
		進出都沒有交代,
		讀者一到那裏就迷路。

		錯誤示例:
		#Qb[
			本文跟着一個網頁請求走一遍:
			從你按下回車,
			到頁面顯示在眼前。
			你在地址欄輸入 `example.com`,
			按下回車。
			瀏覽器不知道這個名字對應哪臺服務器,
			第一件事是查它的地址。

			DNS 誕生於 1983 年,
			設計目的是取代手工維護的主機表。
			DNS 採用分層結構,
			根域、頂級域、權威域逐級授權。
			查詢方式分兩種:
			遞歸查詢把問題整個交給服務器代辦;
			迭代查詢則是逐層問路,
			答案自己彙總。
			記錄類型也多樣,
			A 記錄、CNAME 記錄、MX 記錄各有用途。

			查到地址後,
			瀏覽器與服務器建立連接。
		]
		讀者讀到中段便會問:
		#[「我明明在看這次請求的旅程,
			現在讀的是 DNS 的通用介紹,
			這些內容和這次請求有甚麼關係?」]

		正確示例:
		#Qb[
			本文跟着一個網頁請求走一遍:
			從你按下回車,
			到頁面顯示在眼前。
			你在地址欄輸入 `example.com`,
			按下回車。
			瀏覽器不知道 `example.com` 的服務器在哪,
			第一件事是查它的地址,
			這一步叫域名解析。
			瀏覽器先翻自己的緩存,
			緩存裏沒有,
			就把名字交給本地 DNS 服務器;
			本地服務器也不知道,
			就逐層向上問,
			直到有服務器給出答案。
			地址到手,
			瀏覽器與服務器建立連接。
		]
		同樣講了 DNS,
		由「瀏覽器不知道服務器在哪」把它帶出來,
		只講這次請求會用到的部分,
		講完用「地址到手」送回主線。
	]
]

#H[多舉例子 結合例子說明][
	示例:
	#Qb[問: 等差數列怎麼求和?]

	錯誤示例:
	#Qb[答: 首項加末項乘項數除以2。]

	正確示例:
	#Qb[答: 首項加末項乘項數除以2。
		例如 2, 4, 6, 8, 10這個等差數列,
		該數列的首項爲2,
		末項爲10,
		因爲一共有5項故項數爲5,
		故該數列的和爲: `(2+10)*5/2=30`
	]
]

#H[類比與比喻][
	- #[僅在適當的時間使用,
			不要濫用。]
	- #[濫用的比喻、不恰當的比喻增加理解成本、降低深度。]
	- #[類比要恰當,
			多用同類的事物來類比,
			少用抽象的比喻。]
	正確例:
	#Qb[C\#的`Task`就相當於js的`Promise`✅ 恰當的類比]

	錯誤例:
	#Qb[垃圾回收就像一個勤勞的清潔工每天打掃衛生。]
]

#H[排版][
	文章除了內容上要易懂之外,
	在排版上也要下功夫,
	讓人看得舒服。

	#H[注意分行分段][
		該分行分段就分,
		避免一路小句寫到尾。

		錯誤示例:
		#Qb[
			緩存用來減少數據庫的壓力。
			讀取數據時先查緩存,
			命中就直接返回;
			未命中才查數據庫,
			並把結果寫回緩存。
			緩存也有代價。
			它佔用內存,
			而且數據可能過期,
			所以要設置失效策略,
			否則會讀到舊值。
		]
		正確示例:
		#Qb[
			緩存用來減少數據庫的壓力。
			讀取數據時先查緩存,
			命中就直接返回;
			未命中才查數據庫,
			並把結果寫回緩存。

			緩存也有代價。
			它佔用內存,
			而且數據可能過期,
			所以要設置失效策略,
			否則會讀到舊值。
		]
	]
	#H[善用列表與加粗][
		可採用列表、加粗等語法讓文章更有層次

		錯誤示例:
		#Qb[
			其一, xxx; 其二, yyy
		]
		正確示例:
		#Qb[
			+ xxx
			+ yyy
		]
	]

	#H[引文 要寫明 突出][
		錯誤示例:
		- #[
				快照的內容大意是：
				項目名:「juice-shop 練手」;
				一條資產 `http://127.0.0.1:3001` 還沒測;
				零個攻擊手在運行;
				並發上限是七;
				可用的攻擊手提示詞只有一份, 名字叫kimi-security
				模型能看見的只有這段發過去的文字，
				系統內部存了什麼數據，
				模型一無所知。
			]
		正確示例:
		- #[
				快照的內容大意是：
				#Qb[
					- 項目名:「juice-shop 練手」;
					- 一條資產 `http://127.0.0.1:3001` 還沒測;
					- 零個攻擊手在運行;
					- 並發上限是七;
					- 可用的攻擊手提示詞只有一份, 名字叫kimi-security
				]
				模型能看見的只有這段發過去的文字，
				系統內部存了什麼數據，
				模型一無所知。
			]
		注: 如果你使用typst,
		在typst中,
		引用格式語法爲:
		```typst
		#quote(block:true)[
			引文內容
		]
		```
	]
]

#H[綜合示例][
	#let Nt(cont) = {
		// set text(red)
		// "{"+cont+"}"
	}
	//原文中的`{批註文字}`表示批註
	//先不顯示批註了。前文已經說得很清楚了。直接看正誤示例對照
	+ #[
			#[錯誤示例
				#Qb[
					此工作區是 MyProj——面向某某場景的多 Agent 協作平臺, #Nt[不應使用破折號]
					Bun monorepo, #Nt[缺謂語]
					基於 pi-coding-agent SDK。
					某某比賽一等獎項目（1/114514）。 #Nt[（1/114514）意義不明]
					*公司項目，雙倉庫*: #Nt[名詞短語即成句 缺句子成分]
					GitHub 開源側 `company-org/MyProj`（公司 org）; #Nt[名詞短語即成句 缺句子成分]
					公司內部側 `FireMyProj`（gitlab 託管、非 GitHub，歷史與開源側完全獨立，最新改動都在此。用戶是公司員工、接手負責開發）。 #Nt[濫用括號]
					*主開發場（2026-09-18 起）*：
					工作區內 FireMyProj 的 `dev` 分支
				]
			]
			#[正確示例
				#Qb[
					此工作區屬於 MyProj 項目。
					MyProj 是面向某某場景的多 Agent 協作平臺，
					採用 Bun monorepo 結構，
					基於 pi-coding-agent SDK。
					MyProj 在某某比賽中獲得一等獎，
					在 114514 個參賽項目中排名第一。

					MyProj 是公司項目，
					對應兩個遠端倉庫：
					- #[GitHub 開源側的倉庫名爲 `company-org/MyProj`,
							爲公司的開源倉庫；
						]
					- #[公司內部側的倉庫名爲`FireMyProj`,
							它託管在公司內部的 gitlab 上；
							與 GitHub 開源側沒有共同的提交歷史。
							用戶是公司員工，
							接手負責開發。]

					公司的最新改動都在公司內部側倉庫，
					因此從 2026-09-18 起，
					主開發場是工作區內 FireMyProj 的 `dev` 分支。
				]
			]
			#if false {
				[
					#[違規點對照
						+ #[「MyProj——面向某某場景」
								- 違反「禁止使用破折號」
								- #[破折號刪去,
										拆成兩個完整句子]
							]
						+ #[「Bun monorepo」「某某比賽一等獎項目」「*公司項目，雙倉庫*」「工作區內 FireMyProj 的 `dev` 分支」
								- 違反「不要缺謂語」
								- #[四處都是名詞短語直接當句子,
										各自補上謂語]
							]
						+ #[四個話題擠在一段
								- 違反「注意分行分段」
								- 按話題拆段
							]
						+ #[兩個倉庫的信息用分號串在一句裏,
								- 託管方、開發人員等修飾語全塞在括號裏,
								- 違反「禁止濫用引號或括號作解釋說明」
								- #[改用引導句「對應兩個遠端倉庫」帶出列表,
										修飾語寫成列表項]
							]
						+ #[「最新改動都在此」
								- 違反「指代要明確」
								- 「此」指名爲「公司內部側倉庫」
							]
						+ #[「最新改動都在此」與「*主開發場*」兩句之間
								- 違反「注重邏輯連貫, 注重過渡」
								- 補因果連接詞「因此」
							]
						+ #[「歷史與開源側完全獨立」
								- 違反「注重邏輯連貫, 注重過渡」
								- #[展開寫成正文;
										「歷史與開源側完全獨立」改成「與 GitHub 開源側沒有共同的提交歷史」]
							]
						+ #[（1/114514）
								- 違反「不使用不明確的縮寫/代號」
								- 展開寫成正文
							]
					]
				]
			}
		]//~
	+ #[
			#[錯誤示例
				#Qb[
					爲甚麼分這兩層： #Nt[濫用引號]
					動手的活是模型驅動的， #Nt[濫用口語]
					下一條命令不可預先審查， #Nt[含義不明] #Todo[應給此類錯誤單獨立項][]
					不能讓它直接在宿主上跑； #Nt[它 指代不明]
					一個 `solver` 一個 容器， #Nt[缺句子成分]
					誰把環境打掛了不牽連別人和宿主； #Nt[問題太多不便列舉]
					環境一次性， #Nt[缺句子成分]
					裝過什麼、留下什麼垃圾都隨容器消失。 #Nt[濫用口語/自造抽象術語]
				]
			]
			#[正確示例
				#Qb[
					先講爲什麼要分成兩層。
					因爲攻擊工作由模型驅動，
					下一條命令在事前根本不存在，
					想審查也沒有對象，
					所以這樣的命令不能直接放在宿主上運行；
					一個 `solver` 配一個容器，
					即使任何一方把環境損壞，
					也不會牽連其他人和宿主；
					環境是一次性的，
					容器裏安裝過什麼、留下什麼垃圾，
					都隨着容器一起消失。
				]
			]
	]//~
	+ #[
			#[錯誤示例
				#Qb[
					派工決策到了 `runtime` 手裏才落地：
					構建鏡像、起容器、回收容器。
					一個 `solver` 一個容器，
					一次任務一個隔離沙箱：
					打掛互不影響，
					環境用完即拋。
					起容器當場，
					宿主把任務書拼好、隨啓動命令送進去。
					任務書是中文模板，
					拼五樣：
					項目名與描述；
					目標入口清單，
					每條帶指紋；
					情報池兩塊看板與已交報告的三段摘要；
					行爲要求清單；
					最後把開工便條以「最高優先級遵循」附在末尾。
				]
			]
			#[正確示例
				#Qb[
					派工決策由 `runtime` 負責執行。
					`runtime` 做三件事：
					構建鏡像、創建容器、回收容器。
					一個 `solver` 配一個容器，
					每次任務都使用一個全新的隔離沙箱：
					任何一方把環境損壞，
					都不牽連其他人和宿主；
					環境是一次性的，
					用過之後隨容器一起丟棄。
					容器起動的同時，
					宿主把任務書拼好，
					隨啓動命令一起送進容器。
					任務書是一份中文模板，
					由五部分組成：
					+ 項目名與描述
					+ #[目標入口清單,
							每條帶指紋]
					+ 情報池兩塊看板與已交報告的三段摘要
					+ 行爲要求清單
					+ #[開工便條,
							附在任務書末尾,
							標着最高優先級]
				]
			]
	]//~
]

#H[重構表述][
	重構表述 即 把有問題的文本改寫成表述清晰的文本。

	重構表述的要點:
	- 逐條檢查是否滿足上述要求
	- 站在人類讀者的角度審視是否易懂? 是否有問題?
	- 重構後的文本是否缺少必要的信息量?

	#H[重構不應減少有效信息量][
		重構表述只是換一種表述方法,
		重構後該有的有用信息量應該保持不變。
		對于原文中不必要的冗餘的內容,
		纔可以考慮刪除。
	]
]

#H[自查][
	問自己兩個問題:
	- 是否滿足了上述要求:
	- 站在人類讀者的角度 是否易懂? 是否有問題?
]


#if false{[
	//標識符太多 暫不用此作例
		+ #[
				錯誤示例:
				#Qb[
					pi 會話在容器內的落地點。
					`resolvePromptSession` 拿組裝好的參數。
					createAgentSession 建會話。
					bindExtensions 激活擴展（MCP 適配器也在這步生效）。
					subagent 同一套流程另起。
					startup.json 把組裝參數原樣落盤，排查「這個 `solver` 到底帶了什麼配置」看它。
				]
			]
]}