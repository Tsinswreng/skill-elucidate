#import "@preview/tsinswreng-auto-heading:0.1.0": auto-heading
#let H = auto-heading;
#import "_Common.typ": *
#show: _Show
\-\-\-

name: tsinswreng-elucidate

description: 如何把東西解釋得易懂

\-\-\-
#let Qb(Cont) = quote(block: true)[#Cont]
#H[使用規範的現代漢語書面語][
	#H[句子結構要完整][
		不能缺少該有的成分。

		#H[不要缺主語][
			錯誤示例:
			#Qb[
				❌點擊保存按鈕後,
				會把修改保存到數據庫。
			]

			正確示例:
			#Qb[
				✅用戶點擊保存按鈕後,
				系統會把修改保存到數據庫。
			]
		]

		#H[不要缺謂語][
			錯誤示例:
			#Qb[
				此項目名叫MyProj,
				公司項目。
			]
			正確示例:
			#Qb[
				此項目名叫MyProj,
				*是*公司項目。
			]

		]

		#H[禁止使用破折號][
			//TODO 缺例
		]

		#H[禁止濫用引號或括號作解釋說明][
			該用文字寫清楚就寫清楚。
			//TODO 缺例
		]

	]

	#H[規範用詞][
		#[不要強行把雙字詞砍成單字。]
		錯誤示例:
		- #[❌此函數負責理該數組,
				完成後返回結果。]
		正確示例:
		- #[✅此函數負責處理該數組,
				完成後返回結果。]
	]
	#H[指代要明確][
		#[避免濫用「他」「其」「這」「前/後者」等代詞,
			尤其是在上下文中概念較多時。
			即使你做到了指代無歧義,
			濫用代詞依然會增大讀者的閱讀負擔。
			應指明對象。]

		錯誤示例:
		- #[❌服務器收到請求後把它交給後端,
				後端處理它,
				再把它返回給前端。]
		正確示例:
		- #[✅服務器收到請求後把請求轉交給後端,
				後端處理該請求,
				再把處理結果返回給前端。]
			指代對象要指名詞,
			讀者不用回頭猜「它」是誰。
	]

	#H[說清動作對象][
		如 回覆、傳達 等詞,
		說清是誰給誰。
	]

	#H[避免自造抽象術語][
		錯誤示例:
		- #[❌這兩個模塊的「咬合度」不夠,
				導致改動很痛苦。]
		正確示例:
		- #[✅這兩個模塊耦合過高,
				改動一個常常牽連另一個。]
	]

	#H[避免過度口語化][

	]

	#H[除非術語或專門稱呼,
		避免夾雜英文][
		錯誤示例:
		- ❌調用方只依賴抽象, 不care內部實現細節
		正確示例:
		- ✅調用方只依賴抽象, 不關心內部實現細節
	]
	#H[避免憑空否定][
		先直接看例子:

		#[
			問: 你是哪國人?

			錯誤示例:
			- #[❌答: 我不是日本人,
					不是韓國人,
					而是中國人。]

			正確示例:
			- #[✅答: 我是中國人。]
		]

		讓你介紹一樣東西時
		你就直接說「他是甚麼」/「他怎麼樣」,
		若沒人提到xxx 你就不要憑空否定,
		不要說「他不是xxx而是yyy」。


		僅在需要澄清真實的誤解或混淆項時纔使用,
		禁止做無意義的[憑空捏造出一樣東西再去否定]。

		判別標準:
		被否定的那個理解必須是已經出現過的,
		或確確實實容易引起混淆的。

		例:
		- #[
				問: 我看你長得像東亞人, 你是日本人嗎?

				✅答: 我不是日本人。我是中國人
			]

		- #[
				問: 軟刪除是甚麼

				- #[答: ✅軟刪除是把數據中的記錄標記爲已刪除, 而不會真的物理刪數據的記錄
						✅ 先說是甚麼, 再否認易誤解項]

				- #[答: ❌軟刪除不會真的物理刪數據的記錄, 而是把數據中的記錄標記爲已刪除
						❌ 問甚麼就應該先答甚麼
					]
			]


	]
]

#H[行文][
	#H[注重邏輯連貫, 注重過渡][
		- #[句子之間、段落之間的因果、順序、轉折關係要寫明,
				不要讓讀者腦補。]
		- #[用「先...再...」「若...則...」「因此」「另一方面」等連接詞點明關係。]
		錯誤示例:
		- #[服務器收到請求。
				緩存命中就返回。
				沒命中就查數據庫。
				數據庫也沒有就返回404。]
		正確示例:
		- #[服務器收到請求後先查緩存。
				若緩存命中,
				直接返回緩存結果;
				若未命中,
				再查數據庫,
				數據庫中也沒有則返回404。]
	]

	#H[明確中心主旨/主題/寫作目的][
		文章邏輯要連貫,
		銜接自然,
		文章的每一部分都應爲中心主旨服務,
		和寫作目的/寫作主題有關聯。
		不能想到甚麼就說甚麼
	]
]

#H[多舉例子 結合例子說明][
	示例:
	問:
	- 等差數列怎麼求和?

	錯誤示例:
	- 答: 首項加末項乘項數除以2。

	正確示例:
	- #[答: 首項加末項乘項數除以2。
			例如 2, 4, 6, 8, 10這個等差數列,
			該數列的首項爲2,
			末項爲10,
			因爲一共有5項故項數爲5,
			故該數列的和爲: `(2+10)*5/2=30`
		]
]

#H[類比與比喻][
	- #[僅在適當的時間使用,
			不要濫用。]
	- #[濫用的比喻、不恰當的比喻增加理解成本、降低深度。]
	- #[類比要恰當,
			多用同類的事物來類比,
			少用抽象的比喻。]
	正確例:
	- C\#的Task就相當於js的Promise ✅ 恰當的類比

	錯誤例:
	- #[垃圾回收就像一個勤勞的清潔工每天打掃衛生。]
]

#H[排版][
	文章除了內容上要易懂之外,
	在排版上也要下功夫,
	讓人看得舒服。

	#H[注意分行分段][
		該分行分段就分,
		避免一路小句寫到尾。

		錯誤示例:
		- #[
				緩存用來減少數據庫的壓力。
				讀取數據時先查緩存,
				命中就直接返回;
				未命中才查數據庫,
				並把結果寫回緩存。
				緩存也有代價。
				它佔用內存,
				而且數據可能過期,
				所以要設置失效策略,
				否則會讀到舊值。
			]
		正確示例:
		- #[
				緩存用來減少數據庫的壓力。
				讀取數據時先查緩存,
				命中就直接返回;
				未命中才查數據庫,
				並把結果寫回緩存。

				緩存也有代價。
				它佔用內存,
				而且數據可能過期,
				所以要設置失效策略,
				否則會讀到舊值。
			]
	]
	#H[善用列表與加粗][
		可採用列表、加粗等語法讓文章更有層次

		錯誤示例:
		- #[
				其一, xxx; 其二, yyy
			]
		正確示例:
		- #[
				+ xxx
				+ yyy
			]
	]

	#H[引文 要寫明 突出][
		錯誤示例:
		- #[
				快照的內容大意是：
				項目名:「juice-shop 練手」;
				一條資產 `http://127.0.0.1:3001` 還沒測;
				零個攻擊手在運行;
				並發上限是七;
				可用的攻擊手提示詞只有一份, 名字叫kimi-security
				模型能看見的只有這段發過去的文字，
				系統內部存了什麼數據，
				模型一無所知。
			]
		正確示例:
		- #[
				快照的內容大意是：
				#Qb[
					- 項目名:「juice-shop 練手」;
					- 一條資產 `http://127.0.0.1:3001` 還沒測;
					- 零個攻擊手在運行;
					- 並發上限是七;
					- 可用的攻擊手提示詞只有一份, 名字叫kimi-security
				]
				模型能看見的只有這段發過去的文字，
				系統內部存了什麼數據，
				模型一無所知。
			]
		注: 如果你使用typst,
		在typst中,
		引用格式語法爲:
		```typst
		#quote(block:true)[
			引文內容
		]
		```
	]
]

#H[綜合示例][
	#let Note(cont) = { cont }
	+ #[
			- #[錯誤示例
					#Qb[
						此工作區是 MyProj——面向某某場景的多 Agent 協作平臺,
						Bun monorepo,
						基於 pi-coding-agent SDK。
						某某比賽一等獎項目（1/613）。
						*公司項目，雙倉庫*:
						GitHub 開源側 `company-org/MyProj`（公司 org）;
						公司內部側 `FireMyProj`（gitlab 託管、非 GitHub，歷史與開源側完全獨立，最新改動都在此。用戶是公司員工、接手負責開發）。
						*主開發場（2026-09-18 起）*：
						工作區內 FireMyProj 的 `dev` 分支
					]
				]
			- #[正確示例
					#Qb[
						此工作區屬於 MyProj 項目。
						MyProj 是面向某某場景的多 Agent 協作平臺，
						採用 Bun monorepo 結構，
						基於 pi-coding-agent SDK。
						MyProj 在某某比賽中獲得一等獎，
						在 613 個參賽項目中排名第一。

						MyProj 是公司項目，
						對應兩個遠端倉庫：
						- #[GitHub 開源側的倉庫名爲 `company-org/MyProj`,
								爲公司的開源倉庫；
							]
						- #[公司內部側的倉庫名爲`FireMyProj`,
								它託管在公司內部的 gitlab 上；
								與 GitHub 開源側沒有共同的提交歷史。
								用戶是公司員工，
								接手負責開發。]

						公司的最新改動都在公司內部側倉庫，
						因此從 2026-09-18 起，
						*主開發場*是工作區內 FireMyProj 的 `dev` 分支。
					]
				]
			- #[違規點對照
					#Note[
						每條格式:
						錯誤處 → 改法 → 違反的規則。
						- #[「MyProj——面向某某場景」
								→ 破折號刪去,
								拆成兩個完整句子
								→ 違反「禁止使用破折號」]
						- #[「Bun monorepo」「某某比賽一等獎項目」「*公司項目，雙倉庫*」「工作區內 FireMyProj 的 `dev` 分支」
								→ 四處都是名詞短語直接當句子,
								各自補上謂語
								→ 違反「不要缺謂語」]
						- #[四個話題擠在一段
								→ 按話題拆成三段
								→ 違反「注意分行分段」]
						- #[兩個倉庫的信息用分號串在一句裏,
								託管方、開發人員等修飾語全塞在括號裏,
								→ 改用引導句「對應兩個遠端倉庫」帶出列表,
								修飾語寫成列表項
								→ 違反「善用列表與加粗」「禁止濫用引號或括號作解釋說明」]
						- #[「最新改動都在此」
								→ 「此」指名爲「公司內部側倉庫」
								→ 違反「指代要明確」]
						- #[「最新改動都在此」與「*主開發場*」兩句之間
								→ 補因果連接詞「因此」
								→ 違反「注重邏輯連貫, 注重過渡」]
						- #[「（1/613）」「歷史與開源側完全獨立」
								→ 「（1/613）」展開寫成正文;
								「歷史與開源側完全獨立」改成「與 GitHub 開源側沒有共同的提交歷史」
								→ 違反「注重邏輯連貫, 注重過渡」]
					]
				]
		]
]

#H[自查][
	問自己兩個問題:
	- 是否滿足了上述要求:
	- 站在人類讀者的角度 是否易懂? 是否有問題?
]


